// The definition supermacro of the application configuration with default values.
/* CONFIG(section, property, default) */
CONFIG(window, x, "800")
CONFIG(window, y, "600")
CONFIG(graphics, vsync, "false")
#undef CONFIG 