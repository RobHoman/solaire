// The definition supermacro of the application configuration with default values.
/* CONFIG(section, property, type, default) */
CONFIG(window, x, int, 800)
CONFIG(window, y, int, 600)
CONFIG(graphics, vsync, bool, false)
#undef CONFIG 